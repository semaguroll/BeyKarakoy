<Window x:Class="BeyKarakoyWPF.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeyKarakoyWPF"
           xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"   
        mc:Ignorable="d"
        Title="SearchWindow" Height="450" Width="800">
    <Grid>
        <Button Name="btnClose" HorizontalAlignment="Right" VerticalAlignment="Top" Background="{x:Null}" BorderBrush="{x:Null}" Height="50" Width="50" Margin="0,20,20,0" Click="btnClose_Click" >
            <materialDesign:PackIcon Kind="Clear" Foreground="Black"/>
        </Button>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Width="300">
            <TextBlock HorizontalAlignment="Left" Text="translation missing: tr.general"  Foreground="Gray" FontSize="15" Background="Gainsboro" Width="250" Height="30" VerticalAlignment="Top" FontFamily="Yu Gothic" FontStyle="Italic"/>
            <Button HorizontalAlignment="Right" Width="50" Height="30" Background="Black">
                <materialDesign:PackIcon Kind="Search"/>
            </Button>
        </Grid>
    </Grid>
</Window>
